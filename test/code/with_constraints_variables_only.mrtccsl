assume {
    var global: clock;
    var j: integer;
} structure {
    var o: clock;
    var a: clock;
    var b: clock;
    var c: clock;
    var d: clock;
    var i: integer;
    var a1: clock;
    var a0: clock;
    var a5: clock;
    sensor {
        var start: clock;
        var end: clock;
    };
    controller {
        var start: clock;
        var end: clock;
    };
    actuator {
        var start: clock;
        var end: clock;
    };
} assert {
}