(executable
 (public_name ccsl+)
 (name ccslplus)
 (modules ccslplus common reaction simulate convert)
 (libraries mrtccsl std domainslib mrtccslparsing cmdliner dune-build-info))

(env
 (dev
  (flags
   (:standard -warn-error -A)))
 (release
  (ocamlopt_flags
   (:standard -O3))))
